{
  "name": "lynx",
  "version": "1.0.0",
  "main": "static/index.js",
  "scripts": {
    "watch": "npx esbuild 'src/**/*.ts' --watch=forever --outdir=static/build/js --format=esm",
    "build": "npx esbuild 'src/**/*.ts' --outdir=static/build/js --format=esm --minify",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": ".."
  },
  "author": "Rustum Zia",
  "license": "MIT",
  "description": "",
  "dependencies": {
    "seedrandom": "^3.0.5"
  },
  "devDependencies": {
    "esbuild": "^0.25.1",
    "eslint": "^9.22.0",
    "typescript": "^5.8.2",
    "typescript-eslint": "^8.26.0"
  }
}
